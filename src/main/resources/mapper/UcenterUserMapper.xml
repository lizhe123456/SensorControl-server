<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlcm.server.dao.user.UcenterUserDao">
    <sql id="baseSQL">
        <if test="user_id != null">
            user_id,
        </if>
        <if test="username != null">
            username,
        </if>
        <if test="password != null">
            password,
        </if>
        <if test="state != null">
            state,
        </if>
        <if test="salt != null">
            salt,
        </if>
        <if test="register_time != null">
            register_time,
        </if>
        <if test="register_ip != null">
            register_ip,
        </if>
        <if test="last_login_time != null">
            last_login_time,
        </if>
        <if test="last_login_ip != null">
            last_login_ip,
        </if>
    </sql>
    <sql id="sql_value">
        <if test="user_id != null">
            #{user_id,jdbcType=INTEGER},
        </if>
        <if test="username != null">
            #{username,jdbcType=VARCHAR},
        </if>
        <if test="password != null">
            #{password,jdbcType=VARCHAR},
        </if>
        <if test="state != null">
            #{state,jdbcType=TINYINT},
        </if>
        <if test="salt != null">
            #{salt,jdbcType=VARCHAR},
        </if>
        <if test="register_time != null">
            #{register_time,jdbcType=TIMESTAMP},
        </if>
        <if test="register_ip != null">
            #{register_ip,jdbcType=VARCHAR},
        </if>
        <if test="last_login_time != null">
            #{last_login_time,jdbcType=TIMESTAMP},
        </if>
        <if test="last_login_ip != null">
            #{last_login_ip,jdbcType=VARCHAR},
        </if>
    </sql>

    <insert id="save" useGeneratedKeys="true" keyProperty="user_id" parameterType="com.zlcm.server.model.user.UcenterUser">
        insert into ucenter_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <include refid="baseSQL"/>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <include refid="sql_value"/>
        </trim>
    </insert>

    <update id="update" parameterType="com.zlcm.server.model.user.UcenterUser">
        update ucenter_user
        set
        <if test="username != null">
            username=#{username,jdbcType=VARCHAR},
        </if>
        <if test="password != null">
            password=#{password,jdbcType=VARCHAR},
        </if>
        <if test="state != null">
            state=#{state,jdbcType=TINYINT},
        </if>
        <if test="salt != null">
            salt= #{salt,jdbcType=VARCHAR},
        </if>
        <if test="register_time != null">
            register_time=#{register_time,jdbcType=TIMESTAMP},
        </if>
        <if test="register_ip != null">
            register_ip= #{register_ip,jdbcType=VARCHAR},
        </if>
        <if test="last_login_time != null">
            last_login_time=#{last_login_time,jdbcType=TIMESTAMP},
        </if>
        <if test="last_login_ip != null">
            last_login_ip=#{last_login_ip,jdbcType=VARCHAR},
        </if>
        <if test="locked != null">
            locked = #{locked,jdbcType=TINYINT}
        </if>
        where user_id = #{user_id,jdbcType=INTEGER}
    </update>

    <delete id="deleteByPK" parameterType="java.lang.Integer">
        DELETE FROM ucenter_user WHERE user_id = #{pk,jdbcType=VARCHAR}
    </delete>
    <select id="get" parameterType="java.lang.Integer" resultType="com.zlcm.server.model.user.UcenterOauth">
        SELECT * FROM ucenter_user WHERE user_id = #{pk,jdbcType=VARCHAR}
    </select>
    <select id="findAll" resultType="com.zlcm.server.model.user.UcenterUser">
        select *
        from ucenter_user;
    </select>
    <select id="getCount" resultType="java.lang.Integer">
        select count(*)
        from ucenter_user;
    </select>
    <select id="getPageList" resultType="com.zlcm.server.model.user.UcenterUser">
        select *
        from ucenter_user LIMIT #{page,jdbcType=INTEGER},#{size,jdbcType=INTEGER}
    </select>

    <select id="userNameForSelect"  parameterType="java.lang.String" resultType="com.zlcm.server.model.user.UcenterUser">
        SELECT * FROM ucenter_user WHERE username = #{username,jdbcType=VARCHAR}
    </select>
    <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
</mapper>