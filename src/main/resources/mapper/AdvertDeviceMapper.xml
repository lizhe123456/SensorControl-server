<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlcm.server.dao.AdvertDeviceMapper">
  <resultMap id="BaseResultMap" type="com.zlcm.server.model.bean.AdvertDevice">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="aid" jdbcType="INTEGER" property="aid" />
    <result column="did" jdbcType="INTEGER" property="did" />
  </resultMap>
  <sql id="Base_Column_List">
    id, aid, did
  </sql>
  <select id="get" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from zlcm_advert_device
    where id = #{id,jdbcType=INTEGER}
  </select>
  <delete id="deleteByPK" parameterType="java.lang.Integer">
    delete from zlcm_advert_device
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <insert id="save" parameterType="com.zlcm.server.model.bean.AdvertDevice">
    insert into zlcm_advert_device
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="id != null">
        id,
      </if>
      <if test="aid != null">
        aid,
      </if>
      <if test="did != null">
        did,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="id != null">
        #{id,jdbcType=INTEGER},
      </if>
      <if test="aid != null">
        #{aid,jdbcType=INTEGER},
      </if>
      <if test="did != null">
        #{did,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  <update id="update" parameterType="com.zlcm.server.model.bean.AdvertDevice">
    update zlcm_advert_device
    <set>
      <if test="aid != null">
        aid = #{aid,jdbcType=INTEGER},
      </if>
      <if test="did != null">
        did = #{did,jdbcType=INTEGER},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>
  <select id="findAll" resultType="java.util.List">
    SELECT * FROM zlcm_advert_device
  </select>

  <select id="getCount" resultType="java.lang.Integer">
    SELECT COUNT(*) FROM zlcm_advert_device
  </select>

  <select id="getPageList" parameterType="java.lang.Integer" resultType="com.zlcm.server.model.bean.AdvertDevice">
    SELECT * FROM zlcm_advert_device a  JOIN (select id from zlcm_advert_device limit #{page},#{size}) b ON a.id = b.id
  </select>

  <select id="getDeviceWhereAid" parameterType="java.lang.Integer" resultType="java.lang.Integer">
    SELECT did FROM zlcm_advert_device WHERE aid = #{aid,jdbcType=INTEGER}
  </select>

</mapper>