<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlcm.server.dao.device.UserBindDeviceMapper">
  <resultMap id="BaseResultMap" type="com.zlcm.server.model.device.UserBindDevice">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="did" jdbcType="VARCHAR" property="did" />
    <result column="user_id" jdbcType="INTEGER" property="userId" />
  </resultMap>
  <sql id="Base_Column_List">
    id, did, user_id
  </sql>
  <select id="get" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from user_bind_device
    where id = #{id,jdbcType=INTEGER}
  </select>
  <delete id="deleteByPK" parameterType="java.lang.Integer">
    delete from user_bind_device
    where id = #{id,jdbcType=INTEGER}
  </delete>

  <insert id="save" parameterType="com.zlcm.server.model.device.UserBindDevice">
    insert into user_bind_device
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="id != null">
        id,
      </if>
      <if test="did != null">
        did,
      </if>
      <if test="userId != null">
        user_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="id != null">
        #{id,jdbcType=INTEGER},
      </if>
      <if test="did != null">
        #{did,jdbcType=VARCHAR},
      </if>
      <if test="userId != null">
        #{userId,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  <update id="update" parameterType="com.zlcm.server.model.device.UserBindDevice">
    update user_bind_device
    <set>
      <if test="did != null">
        did = #{did,jdbcType=VARCHAR},
      </if>
      <if test="userId != null">
        user_id = #{userId,jdbcType=INTEGER},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>
  <select id="findAll" resultType="com.zlcm.server.model.device.UserBindDevice">
    select *
    from device;
  </select>
  <select id="getCount" resultType="java.lang.Integer">
    select count(*)
    from device;
  </select>
  <select id="getPageList" resultType="java.lang.Integer">
    select *
    from device LIMIT #{page,jdbcType=INTEGER},#{size,jdbcType=INTEGER}
  </select>
  <select id="unbind" parameterType="java.util.Map">
    DELETE from user_bind_device WHERE user_id = #{uid} AND did = #{did};
  </select>
  <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
</mapper>